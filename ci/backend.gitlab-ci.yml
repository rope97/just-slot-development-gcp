stages:
  - build
  - deploy

variables:
  REGION: europe-west1
  PROJECT_ID: just-slots-499010
  REPOSITORY: game-backend
  IMAGE: $REGION-docker.pkg.dev/$PROJECT_ID/$REPOSITORY/api

# -------------------------
# BUILD
# -------------------------
backend:build:
  stage: build
  image: docker:24
  services:
    - docker:dind
  script:
    - docker build -t $IMAGE:$CI_COMMIT_SHA ./apps/game-backend
    - docker tag $IMAGE:$CI_COMMIT_SHA $IMAGE:latest

    - gcloud auth configure-docker $REGION-docker.pkg.dev -q

    - docker push $IMAGE:$CI_COMMIT_SHA
    - docker push $IMAGE:latest

# -------------------------
# DEPLOY DEV / STAGING / PROD
# -------------------------
backend:deploy:
  stage: deploy
  image: google/cloud-sdk:slim
  script:
    - echo "=== DEPLOY START ==="

    # decide env from branch
    - |
      if [ "$CI_COMMIT_BRANCH" = "dev" ]; then
        ENV="dev"
      elif [ "$CI_COMMIT_BRANCH" = "staging" ]; then
        ENV="staging"
      elif [ "$CI_COMMIT_BRANCH" = "main" ]; then
        ENV="production"
      else
        echo "No deploy for this branch"
        exit 0
      fi

    # auth
    - gcloud config set project $PROJECT_ID
    - gcloud container clusters get-credentials cluster-dev --region us-central1

    # deploy
    - kubectl set image deployment/game-backend-$ENV backend=$IMAGE:$CI_COMMIT_SHA
    - kubectl rollout status deployment/game-backend-$ENV

    # smoke test
    - |
      if [ "$ENV" = "dev" ]; then
        URL="http://api-dev-justslots.duckdns.org"
      elif [ "$ENV" = "staging" ]; then
        URL="http://api-staging-slots.duckdns.org"
      else
        URL="http://api-prod-slots.duckdns.org"
      fi

    - sleep 10
    - curl -f $URL/health || echo "Smoke test failed"

  rules:
    - if: $CI_COMMIT_BRANCH == "dev"
    - if: $CI_COMMIT_BRANCH == "staging"
      when: manual
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual
